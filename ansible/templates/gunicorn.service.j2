[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=root
Group=root
WorkingDirectory={{ web_root }}
ExecStart=/usr/bin/gunicorn --bind unix:{{ web_root }}/gunicorn.sock gifPrinterClient.wsgi:application

[Install]
WantedBy=multi-user.target
